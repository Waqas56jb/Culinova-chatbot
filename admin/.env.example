# Admin panel talks to the backend server, which holds the Supabase secret key.
# No Supabase keys are needed here.

# Backend API base (defaults to http://<hostname>:3000/api when unset)
# VITE_API_URL=http://localhost:3000/api
